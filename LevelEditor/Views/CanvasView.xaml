<Page x:Class="LevelEditor.Views.CanvasView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:LevelEditor.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="CanvasView">
    <Page.DataContext>
        <Binding Path="Canvas" Source="{StaticResource Locator}" />
    </Page.DataContext>

    <Grid>

        <Canvas x:Name="CanvasElement" MouseDown="CanvasElement_MouseDown" Background="White">
            
        </Canvas>

    </Grid>
</Page>
